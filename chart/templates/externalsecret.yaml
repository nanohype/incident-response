{{- if .Values.externalSecret.enabled -}}
# Aggregates the incident-response-side secrets into one k8s Secret consumed by webhook
# + processor via envFrom. The HMAC secret has its own AWS entry (cached by
# the webhook handler keyed on VersionId — see src/handlers/webhook-ingress.ts);
# we pull only its current value here, but the env var
# GRAFANA_ONCALL_HMAC_SECRET_ID (set on the webhook Deployment) is what drives
# the cache-by-version refresh path.

apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "tenant-chart-base.fullname" . }}-secrets
  labels:
    {{- include "tenant-chart-base.labels" . | nindent 4 }}
spec:
  refreshInterval: {{ .Values.externalSecret.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.externalSecret.secretStore }}
    kind: ClusterSecretStore
  target:
    name: {{ include "tenant-chart-base.fullname" . }}-secrets
    creationPolicy: Owner
  data:
    # App-secrets bundle — Slack tokens + Linear + Grafana + Statuspage + WorkOS
    - secretKey: SLACK_BOT_TOKEN
      remoteRef:
        key: {{ .Values.externalSecret.appSecret }}
        property: SLACK_BOT_TOKEN
    - secretKey: SLACK_SIGNING_SECRET
      remoteRef:
        key: {{ .Values.externalSecret.appSecret }}
        property: SLACK_SIGNING_SECRET
    - secretKey: SLACK_APP_TOKEN
      remoteRef:
        key: {{ .Values.externalSecret.appSecret }}
        property: SLACK_APP_TOKEN
    - secretKey: GRAFANA_ONCALL_TOKEN
      remoteRef:
        key: {{ .Values.externalSecret.appSecret }}
        property: GRAFANA_ONCALL_TOKEN
    - secretKey: GRAFANA_CLOUD_TOKEN
      remoteRef:
        key: {{ .Values.externalSecret.appSecret }}
        property: GRAFANA_CLOUD_TOKEN
    - secretKey: GRAFANA_CLOUD_ORG_ID
      remoteRef:
        key: {{ .Values.externalSecret.appSecret }}
        property: GRAFANA_CLOUD_ORG_ID
    - secretKey: STATUSPAGE_API_KEY
      remoteRef:
        key: {{ .Values.externalSecret.appSecret }}
        property: STATUSPAGE_API_KEY
    - secretKey: STATUSPAGE_PAGE_ID
      remoteRef:
        key: {{ .Values.externalSecret.appSecret }}
        property: STATUSPAGE_PAGE_ID
    - secretKey: LINEAR_API_KEY
      remoteRef:
        key: {{ .Values.externalSecret.appSecret }}
        property: LINEAR_API_KEY
    - secretKey: LINEAR_PROJECT_ID
      remoteRef:
        key: {{ .Values.externalSecret.appSecret }}
        property: LINEAR_PROJECT_ID
    - secretKey: LINEAR_TEAM_ID
      remoteRef:
        key: {{ .Values.externalSecret.appSecret }}
        property: LINEAR_TEAM_ID
    - secretKey: WORKOS_API_KEY
      remoteRef:
        key: {{ .Values.externalSecret.appSecret }}
        property: WORKOS_API_KEY
    # Grafana Cloud OTLP basic-auth header (used by both webhook + processor
    # at OTel SDK init)
    - secretKey: OTEL_EXPORTER_OTLP_HEADERS
      remoteRef:
        key: {{ .Values.externalSecret.grafanaCloudSecret }}
        property: otlpAuthHeader
{{- end }}

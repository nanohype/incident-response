{{- if .Values.webhook.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tenant-chart-base.fullname" . }}-webhook
  labels:
    {{- include "tenant-chart-base.labels" . | nindent 4 }}
    incident-response.io/service: webhook
spec:
  replicas: {{ .Values.webhook.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      {{- include "incident-response.selectorLabels" (dict "Values" .Values "Release" .Release "Chart" .Chart "service" "webhook") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "tenant-chart-base.labels" . | nindent 8 }}
        incident-response.io/service: webhook
      annotations:
        # Rolls the pods whenever the ExternalSecret's inputs move — the secret
        # prefix, the projected key list, or the refresh interval. Hashing the
        # whole block means a new remoteRef cannot be added without a restart.
        checksum/external-secret: {{ .Values.externalSecret | toYaml | sha256sum }}
    spec:
      serviceAccountName: {{ include "tenant-chart-base.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: webhook
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: {{ .Values.webhook.command }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.webhook.port }}
              protocol: TCP
          env:
            {{- include "incident-response.env" (dict "Values" .Values "service" "webhook") | nindent 12 }}
            - name: OTEL_SERVICE_NAME
              value: incident-response-webhook
            - name: PORT
              value: {{ .Values.webhook.port | quote }}
            # webhook handler only needs the HMAC secret name; it fetches +
            # caches the value at request time (keyed on VersionId, 5-min TTL).
            # Same Secrets Manager path scripts/seed-secrets.sh writes and
            # scripts/fire-drill.sh signs with — the CI secret-path parity gate
            # asserts it against the seeder's inventory.
            - name: GRAFANA_ONCALL_HMAC_SECRET_ID
              value: {{ printf "%s/%s" .Values.externalSecret.secretPrefix .Values.externalSecret.hmacSecretKey | quote }}
          envFrom:
            - secretRef:
                name: {{ include "tenant-chart-base.fullname" . }}-secrets
          livenessProbe:
            {{- toYaml .Values.probes.webhook.liveness | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.probes.webhook.readiness | nindent 12 }}
          resources:
            {{- toYaml .Values.webhook.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
{{- end }}

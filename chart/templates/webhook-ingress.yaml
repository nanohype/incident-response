{{- if and .Values.ingress.enabled .Values.webhook.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "marshal.fullname" . }}-webhook
  labels:
    {{- include "marshal.labels" . | nindent 4 }}
    marshal.io/service: webhook
  annotations:
    {{- if .Values.ingress.tls.enabled }}
    cert-manager.io/cluster-issuer: {{ .Values.ingress.tls.issuer | quote }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.ingress.host | quote }}
      secretName: {{ include "marshal.fullname" . }}-webhook-tls
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          - path: {{ .Values.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "marshal.fullname" . }}-webhook
                port:
                  number: {{ .Values.webhook.port }}
{{- end }}
